version: '3.8'
services:
  rl-training:
    build: .
    volumes:
      - ./output:/src/output
    environment:
      - PYTHONUNBUFFERED=1
      - MPLBACKEND=Agg
    mem_limit: 6g
    memswap_limit: 12g